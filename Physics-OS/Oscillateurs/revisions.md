```
x(t) = Xm * cos(wt + 0)
v(t) = -Xm * w * sin(2t + 0)
a(t) = -Xm * w² * cos(wt + 0)

Xm = Amplitude
w  = fréquence angulaire
0  = déphasage

w = 2pi / t
```
```
Frap = -k * x
a = -w * x
F = ma

w = racine(k/m) (faut démontrer)

Frap = Force de rappel
k    = raideur
a    = accélération
```

